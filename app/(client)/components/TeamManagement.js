'use client'

// Use the integrated dashboard with simplified navigation (2 tabs only)
import IntegratedTeamDashboard from './IntegratedTeamDashboard'

export default function TeamManagement(props) {
  return <IntegratedTeamDashboard {...props} />
}